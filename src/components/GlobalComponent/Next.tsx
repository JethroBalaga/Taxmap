import React from 'react';
import { IonButton } from '@ionic/react';

const Next: React.FC = () => {
  return (
    <IonButton expand="block" color="primary">
      Next
    </IonButton>
  );
};

export default Next;
